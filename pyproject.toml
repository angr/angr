[build-system]
requires = ["setuptools>=59", "wheel", "pyvex==9.2.26.dev0", "unicorn==1.0.2rc4", "unicorn @ git+https://github.com/unicorn-engine/unicorn.git@dev#subdirectory=bindings/python"]
build-backend = "setuptools.build_meta"
