from distutils.core import setup

setup(
    name='identifier',
    version='1.0',
    description='The function identifier',
    packages=['identifier'],
    install_requires=[
        'angr',
    ],
)
