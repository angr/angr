memlist.txt contains the sequence in which pages are allocated using "allocate" in CGC.
The list has been generated using the CGC binary "memtest" in the DECREE vm.
Ideally tracer/QEMU should behave the same.

